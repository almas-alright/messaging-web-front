export function App() {
  return (
    <main className="app-shell">
      <section className="welcome-panel">
        <p className="eyebrow">Messaging demo</p>
        <h1>Reusable chat frontend</h1>
        <p>
          Vite, React, and TypeScript are ready for the local messaging service
          demo.
        </p>
      </section>
    </main>
  );
}
