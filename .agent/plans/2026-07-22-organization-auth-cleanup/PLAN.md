# Organization Auth Cleanup — Frontend

Branch: `frontend-organization-auth-cleanup`

Goal: expose only organization email registration, OTP verification, login and workplace chat.

Backend tasks `ORG-AUTH-BE-01` through `ORG-AUTH-BE-05` must finish before frontend contract work is marked complete.

Out of scope: Google Drive, social login, support widget, legacy demo and new product features.

Execute only the first unchecked task. One task equals one commit.
